# Server port (default: 3000)
PORT=3000

# Public URL for the MCP server (used by clients to connect)
MCP_URL=http://localhost:3000

# Set to any value to skip seeding the database with sample data
SKIP_SEED=
